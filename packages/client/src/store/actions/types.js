const API = 'API'

const appTypes = {
  backToState: 'BACK_TO_STATE',
  getGraph: 'GET_GRAPH',
  getCycles: 'GET_CYCLES',
  toggleLoading: 'TOGGLE_LOADING',
}

const selectedTypes = {
  setSelected: 'SET_SELECTED',
}

export {
  appTypes,
  selectedTypes,
  API,
}
